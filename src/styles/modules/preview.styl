.dv-screen {
  height: 100%;
  width: 100%;
  .dv-canvas.fullscreen{
      position:absolute;
      left:50%;
      right:50%;

  }
  .canvas-panel {
    left: 50%;
    top: 50%;
    position: absolute;
    .preview-box {
      position: absolute;
    }
  }
}

.dv-screen .bin-scrollbar__wrap {
  overflow-x: hidden;
}
